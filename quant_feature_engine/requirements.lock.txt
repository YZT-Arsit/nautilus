# Lockfile for quant_feature_engine — exact versions known to pass the
# full test suite + real-data parity sweep on:
#   Windows 10 AMD64 / Python 3.13.13
#   D:\nautilus\.venv  (May 2026)
#
# Regenerate after intentional dep upgrades:
#   ssh nautilus-server "powershell -Command 'cd D:\nautilus; \
#       .\.venv\Scripts\python.exe -m pip install -U -r quant_feature_engine\requirements.txt; \
#       .\.venv\Scripts\python.exe -m pip freeze > quant_feature_engine\requirements.lock.txt'"
# then prune to qfe-relevant rows and re-run the validation harness.
#
# Install from this lockfile (production-grade reproducible install):
#   pip install -r quant_feature_engine/requirements.lock.txt

# --- runtime ---
polars==1.41.1
polars-runtime-32==1.41.1
pyarrow==24.0.0
PyYAML==6.0.3

# --- test ---
pytest==7.4.4
pytest-timeout==2.4.0

# --- pytest transitive ---
colorama==0.4.6
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
